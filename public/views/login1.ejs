<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Register - Step 1</title>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/output.css">
    <style>
        body {
            font-family: 'Roboto', sans-serif;
        }
        .bg-gray-900 {
            background-color: #1a202c;
        }
        .bg-gray-800 {
            background-color: #2d3748;
        }
        .input-field {
            transition: border-color 0.2s;
        }
        .input-field:focus {
            border-color: #63b3ed;
            outline: none;
            box-shadow: 0 0 0 1px #63b3ed;
        }
        .register-box {
            max-width: 400px;
            width: 100%;
            padding: 2rem;
            border-radius: 0.5rem;
            background-color: #2d3748;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.2);
        }
        .register-box > h1{
            align-items: center;
            text-align: center;
        }
        .register-button {
            background-color: #3182ce;
            transition: background-color 0.2s;
            border-radius: 5px;
        }
        .register-button:hover {
            background-color: #2b6cb0;
        }
        .flex-center {
            display: flex;
            align-items: center;
            justify-content: center;
            min-height: 100vh; /* Ensures the content is vertically centered */
        }

        input{
            border-radius: 5px;
        }
    </style>
</head>
<body class="bg-gray-900 flex flex-col md:flex-row min-h-screen">
    <div class="flex-center flex-1">
        <div class="register-box">
            <h1 class="text-2xl text-white font-bold mb-4 text-center ">Register</h1>
            <form id="registerForm">
                <div class="mb-4">
                    <label for="email" class="block text-sm font-medium text-gray-300">Email:</label>
                    <input type="email" name="email" id="email" required
                           class="input-field mt-1 p-2 block w-full bg-gray-700 border border-gray-600 rounded-md text-gray-200">
                </div>

                <div class="mb-4">
                    <label for="password" class="block text-sm font-medium text-gray-300">Password:</label>
                    <input type="password" name="password" id="password" required
                           class="input-field mt-1 p-2 block w-full bg-gray-700 border border-gray-600 rounded-md text-gray-200">
                </div>

                <button type="submit" class="register-button w-full text-white font-semibold py-2 rounded-md transition duration-200">
                    Next
                </button>
            </form>

            <div id="error-message" class="text-red-500 mt-4"></div>
        </div>
    </div>

    <div class="hidden md:flex flex-1 bg-cover bg-center" style="background-image: url('/path/to/your/image.jpg');">
        <!-- You can replace '/path/to/your/image.jpg' with your actual image path -->
    </div>
    <script src="/js/login1.js"></script>
</body>
</html>